class AppConstants {
  static const String baseUrl = 'http://localhost:5000/api'; // Use 10.0.2.2 for Android emulator if needed, but localhost for web/iOS sim
  // Using localhost for now, will need to be configured for device
  
  static const String appName = 'Timely';
  
  // Storage Keys
  static const String tokenKey = 'auth_token';
  static const String userDataKey = 'user_data';
}
